# Homepage: redirect /index and /index.html to canonical /
/index.html   /   301!
/index        /   301

# Force clean URLs: redirect .html to extensionless
/*.html   /:splat   301!

# Serve extensionless URLs from .html files
/*   /:splat.html   200
